# Umbrella 🌂 — dependencies
#
# CORE — zero hard requirements. Works on any machine including CPU/Pi.
# Install only what you need for your use case.

# ── For full TurboQuant validation and compression (CUDA GPU required) ──
# pip install torch>=2.0 turboquant

# ── For HTTP + MCP server mode (umbrella serve) ─────────────────────────
# pip install fastapi uvicorn

# ── For accurate RAM detection in doctor mode ───────────────────────────
# pip install psutil

# ── All at once ──────────────────────────────────────────────────────────
# pip install 'umbrella-tq[all]'

# ── Minimum for suggest / autotune / synthetic validate (no GPU needed) ──
# pip install umbrella-tq
# (no other packages required)
